import { Rule, SchematicContext } from '@angular-devkit/schematics';
import { Schema } from '../../models/Ischema';
export declare function createModule(options: Schema, context: SchematicContext): Rule;
